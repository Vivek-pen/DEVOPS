Perfect 👍 — here’s a clear and compact guide that gives you:

1️⃣ All basic steps to integrate Docker + Kubernetes on Windows
2️⃣ All essential Kubernetes commands you’ll actually use

No confusion, no extra setup — just what’s needed for your practicals or experiments 💪

🧩 PART 1: Integrate Docker and Kubernetes (Windows)
🪟 Step 1: Install Docker Desktop

👉 Download and install from
https://www.docker.com/products/docker-desktop

During installation:

✅ Choose Use WSL2

Keep everything else default

After installation, open Docker Desktop.

⚙️ Step 2: Enable Kubernetes Inside Docker Desktop

Open Docker Desktop

Go to Settings → Kubernetes

✅ Check Enable Kubernetes

Click Apply & Restart

Wait until you see “Kubernetes is running” at the bottom.

That’s it — Kubernetes is now integrated with Docker Desktop! 🎉

🧠 Step 3: Verify Installation

Open PowerShell or Command Prompt and type:

kubectl version --client
kubectl get nodes


✅ Output:

NAME              STATUS   ROLES           AGE   VERSION
docker-desktop    Ready    control-plane   2m    v1.30.x


🎯 You have successfully integrated Docker + Kubernetes.

🐳 PART 2: Run a Containerized App Automatically
Step 1: Create a Simple Deployment
kubectl create deployment myapp --image=nginx

Step 2: Expose It as a Service
kubectl expose deployment myapp --type=NodePort --port=80

Step 3: Check What’s Running
kubectl get all


You’ll see:

pod/myapp-xxxxxxx
service/myapp
deployment.apps/myapp

Step 4: Access the App

Find the port:

kubectl get service


Output:

PORT(S)
80:30001/TCP


👉 Open in browser:

http://localhost:30001


🎉 You’ll see the NGINX welcome page.

☸️ PART 3: All Important Kubernetes Commands
Command	Description
kubectl version	Show Kubernetes version
kubectl get nodes	List all cluster nodes
kubectl get pods	Show all pods
kubectl get services	Show all services
kubectl get deployments	Show deployments
kubectl get all	Show all resources
kubectl describe pod <pod-name>	Detailed info about a pod
kubectl logs <pod-name>	Show container logs
kubectl exec -it <pod-name> -- /bin/bash	Access pod shell
kubectl delete pod <pod-name>	Delete a pod
kubectl delete deployment <name>	Delete deployment
kubectl delete service <name>	Delete a service
kubectl scale deployment <name> --replicas=3	Scale pods up/down
kubectl apply -f deployment.yaml	Deploy using a YAML file
kubectl delete -f deployment.yaml	Remove resources from YAML
kubectl expose deployment <name> --type=NodePort --port=<port>	Expose a deployment
kubectl get events	Show cluster events (debugging)
📦 PART 4: Deploy Your Own Docker Image

If you built your own Docker image (for example myapp:1.0):

docker build -t myapp:1.0 .
kubectl create deployment myapp-deploy --image=myapp:1.0
kubectl expose deployment myapp-deploy --type=NodePort --port=8080
kubectl get service


Access at:

http://localhost:<nodePort>

🧾 PART 5: Stop / Restart Cluster
Command	Description
kubectl delete all --all	Delete all resources
kubectl get all	Check if cluster is empty
(No need to stop manually — Docker Desktop manages it)	Kubernetes auto-starts with Docker
⚡ Quick Recap

✅ Integration:
→ Enable Kubernetes in Docker Desktop settings

✅ Verification:
→ kubectl get nodes

✅ Deploy & Run App:
→ kubectl create deployment ...
→ kubectl expose deployment ...

✅ Check Everything:
→ kubectl get all

✅ Access App:
→ http://localhost:<NodePort>

Would you like me to give you a single PowerShell automation script that does all these steps (create, expose, verify, and open in browser automatically)?
It’s perfect for labs and demos — just run once and it shows your app live.




minikube start --driver docker